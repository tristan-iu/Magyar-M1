# ============================================================
# requirements.txt — Magyar pipeline (racine du projet)
# Couvre tous les modules : 1a_scraper, 2a_metadonnees, 2b_transcription,
#   2c_traduction, 2d_vision/{keyframes,visages,clip,blasons},
#   3a_lexicometrie, 3d_couleurs, 0_config.
# Chaque module a aussi son requirements.txt minimal.
# Exception : 2b_transcription/audio_segmentation.py demande un venv
# séparé (inaSpeechSegmenter + TensorFlow) — voir son README.
#
# Dependance systeme (hors pip) :
#   sudo apt install ffmpeg
#
# Modele spaCy ukrainien (post-install) :
#   python -m spacy download uk_core_news_trf
# ============================================================

# --- Config / utils ---
PyYAML>=6.0

# --- 1a_scraper ---
telethon>=1.34.0
python-socks[asyncio]>=2.4.0
imagehash>=4.3.0

# --- 2b_transcription (Whisper + VAD) ---
openai-whisper
torch>=2.0
torchaudio
torchcodec  # requis par torchaudio >= 2.9 pour la lecture audio (Silero VAD)

# --- 2d_vision/keyframes (OCR, SceneDetect) ---
easyocr>=1.7
scenedetect[opencv]>=0.6

# --- 2d_vision/clip (CLIP classify) ---
transformers>=4.30
seaborn>=0.12

# --- 2d_vision/visages (InsightFace) ---
insightface>=0.7
onnxruntime-gpu>=1.16

# --- 2c_traduction ---
deepl

# --- 3a_lexicometrie ---
scipy
spacy
scikit-learn
statsmodels>=0.14
gensim>=4.3
pyLDAvis>=3.4
prince           # AFC (afc.py)
networkx         # réseaux de cooccurrences (cooccurrences.py)

# --- Partagees (plusieurs scripts) ---
opencv-python>=4.8.0
Pillow>=10.0.0
numpy>=1.24
pandas>=1.5
matplotlib>=3.7
tqdm>=4.65
python-dotenv>=1.0.0
